# AGENTS.md

Area contains an extracted university report archive.

- Read `main.tex`, `report.sty`, and `sections/` first for the root report variant.
- Bibliography entries live in `references.bib`; do not recreate `sections/Refs.tex`.
- Generated LaTeX artifacts (`*.aux`, `*.log`, `*.toc`, `*.out`, `*.lof`, `*.lot`, `*.synctex.gz`) and preview caches are not source files.
- Keep edits surgical; preserve Vietnamese/English wording unless the user asks for rewriting.
